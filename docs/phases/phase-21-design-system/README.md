# Phase 21: Design System (S21)

## Muc tieu
Dark mode CSS + color palette + typography + base layout.
Nen tang visual cho toan bo dashboard.

## Session 21
- Files: globals.css, components/layout/sidebar.tsx, layout/header.tsx, theme-provider.tsx
- Dark mode: CSS variables, toggle theme
- Colors: professional palette (dark bg, accent colors cho status)
- Typography: Google Fonts (Inter/Roboto)
- Layout: sidebar (nav) + header (user info, notifications) + main content
- Test: layout renders correctly dark mode

## Lien quan PRD: F7 Dashboard localhost
